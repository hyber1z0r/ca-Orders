extends layout
block content
    div.container
        h1 View Categories
        table.table.table-striped.table-hover.table-bordered
            thead
                tr
                    th Name
                    th ID
                    th Description
                    th Edit / Delete
            tbody
            each category in categories
                tr
                    td
                        a(href='/categories/'+ category._id)  #{category.name}
                    td
                        p #{category._id}
                    td
                        p #{category.description}
                    td
                        button.btn.btn-info(type='button', data-toggle='modal', data-target='#editModal', id="editbutton")
                            span.glyphicon.glyphicon-comment
                        button.btn.btn-info(type='button',data-toggle='modal', data-target='#deleteModal', id="deletebutton", rowid=category._id)
                            span.glyphicon.glyphicon-trash

        a(href='/orders', target='_parent').btn.btn-primary
            span.glyphicon.glyphicon-arrow-left &nbsp;Back